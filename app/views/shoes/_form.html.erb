<%= simple_form_for [outfit, shoe] do |f| %>
  <%= f.input :brand %>

  <% if shoe.new_record? %>
    <%= f.submit "Create your shoes!", class: "btn btn-warning" %>
  <% else %>
    <%= f.submit "Edit your shoes!", class: "btn btn-warning" %>
  <% end %>
<% end %>
