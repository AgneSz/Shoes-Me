<%# card to display outfit pic %>

<div class="container">
  <div class="card">
    <% if @outfit.photo.attached? %>
    <%= cl_image_tag @outfit.photo.key, height: 600, width:300, crop: :fill, class: 'outfit-pic' %>
    <% end %>
  </div>
</div>

<%# form to add a shoe to an outfit %>
